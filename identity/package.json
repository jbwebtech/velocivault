{
  "name": "identity-api",
  "version": "1.0.0",
  "main": "identity-api.js",
  "scripts": {
    "build": "tsc",
    "start": "nodemon --ext ts,js,json --ignore ./out --exec \"tsc && node ./out/identity-api.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "nodemon": "^3.1.9"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "typescript": "^5.7.3"
  }
}
